# Command: `textbox`

Controls textbox visibility at runtime.

## Usage
```vnef
textbox show
textbox hide
textbox wait
```

## Behavior
- `show`: makes the textbox visible immediately.
- `hide`: hides the textbox until you show it again.
- `wait`: hides the textbox and waits for a click; on click it shows the textbox and advances one command.
